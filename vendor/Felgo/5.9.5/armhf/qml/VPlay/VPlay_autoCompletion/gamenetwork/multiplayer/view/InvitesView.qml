import VPlay 2.0
import QtQuick 2.0

Item {
  id: friendsSelectorView

  property variant gameNetworkItem

  property variant multiPlayerClient

  signal inviteClicked(variant inviteData)

  signal acceptInviteClicked(variant inviteData)



}
