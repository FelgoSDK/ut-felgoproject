import QtQuick 2.2



QtObject {

  
  function generateRandomValueBetween(minimum, maximum) {
  }

  
  function clamp(num, minimum, maximum) {
  }

  
  function randomColor()
  {
  }

  
  function cropPath(path, cropExtension) {
  }

    
  
  function captureScreenshot(name, x, y, width, height, scene) {
  }

  
  function captureScreenshotFullscreen(name) {
  }

  
  function decimals(number, scientific) {
  }
}
